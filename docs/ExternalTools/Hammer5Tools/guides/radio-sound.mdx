---
title: Looping radio
description: How to prepare and place synchronized looping audio.
image: /img/hammer5tools/icons/radio_sound.png
sidebar_position: 3
---

This guide prepares looping music, ambient radios, and soundscapes for <Game name="cs2"/> with the built-in [Audio Editor](../editors/audio-editor.mdx) and [SoundEvent Editor](../editors/sound-editor.mdx).


## 1. Set up loop marks in Audio Editor

<Game name="cs2"/> uses RIFF Cue Markers stored directly within `.wav` files to determine loop start and loop end boundaries.

1. Switch to the Audio Editor tab in <Tool name="hammer5tools"/>.
2. Double-click the source `.wav` or `.mp3` file in Audio Explorer to open it.
3. Listen to the track and find the loop start and loop end points:
   - Click on the waveform where the loop begins and press `M` (Add Marker).
   - Click on the waveform where the loop ends and press `M` (Add Marker).
4. Marker 1 and Marker 2 now define the loop region.
5. Press Ctrl+S to save the audio file. <Tool name="hammer5tools"/> writes the cue points directly into the WAV header.

:::tip
Use Ramp Up and Ramp Down in the Audio Editor to fade a selected region before saving.
:::


## 2. Create the sound event

1. Switch to the SoundEvent Editor tab.
2. Right-click the SoundEvent hierarchy and choose New, then rename the event (for
   example, `radio.music_loop_01`).
3. Set the property `type = csgo_mega` (or `csgo_music`).
4. In `vsnd_files`, add the saved `.wav` file path.
5. Create a Distance Volume Mapping Curve to configure hearing distance:
   - Near volume: `1.0` (at distance 0–100 units).
   - Far volume: `0.01` (at distance 1500 units).

:::note
Why set min volume to 0.01 instead of 0?
> If a looping sound event reaches 0.0 volume, <Game name="cs2"/> will unload it from player memory, losing its current playback position. Setting min volume to `0.01` keeps the sound synchronizing across all players even when far away.
:::


## 3. Place the entity in Hammer

1. In Hammer Editor, create a `point_soundevent` entity.
2. Set Soundevent Name to the custom event (`radio.music_loop_01`).
3. Set Start On Spawn to `true`.
4. Run or compile the map. The radio music starts and loops in <Game name="cs2"/>.
